import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "HackerStats - Discover the Hackathon Universe",
  description: "The ultimate platform for hackathon data analysis and visualization. Explore projects, track hacker journeys, and unlock insights from the world's largest hackathon database.",
  keywords: "hackathon, data visualization, graph database, neo4j, projects, hackers, analytics",
  authors: [{ name: "HackerStats Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
